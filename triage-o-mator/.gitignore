# What this repository keeps of its triage, and what it doesn't. Generated by triage-o-mator's bin/install-to.

# <triage-o-mator:symlinks> — rewritten by bin/install-to; wired to a checkout, so never committed
/bin
/themes
/docs
/AGENTS.md
/README.md
/prompts/PLAYBOOK.md
/prompts/auto-triage.md
/prompts/find-duplicates.md
/prompts/maintainer-brief.md
/prompts/organize-groups.md
/prompts/polish-report.md
/prompts/prepare-analysis.md
/prompts/review-appeal.md
/prompts/review-pr.md
# </triage-o-mator:symlinks>

# Machine-local: which triage-o-mator checkout this install is wired to.
/.triage-install.json

# Disposable, re-created on demand by bin/fetch, bin/batch, bin/export-csv and bin/group export.
/data/*/*/raw/
/data/*/*/batches/
/data/*/*/exports/

# Local group locking and atomic-write temporary files.
/data/*/*/groups/.lock
/data/*/*/groups/.group-*

# Per-checkout UI preference.
/config/theme.local

# The checkout's version of a config file you have edited, written by bin/install-to to diff yours against.
/config/*.dist

# <triage-o-mator:cache>
# Ignored evidence cache and machine-local transaction state.
/data/*/*/cache/
/data/*/*/local/
# </triage-o-mator:cache>
