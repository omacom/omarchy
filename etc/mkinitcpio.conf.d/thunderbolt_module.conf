# Load thunderbolt early so Plymouth can use some Thunderbolt docks (#1893).
# On hosts without BootACL this can drop firmware-provided dock USB before the
# LUKS prompt (#10453). Opt out with: omarchy-setup-early-thunderbolt disable
MODULES+=(thunderbolt)
