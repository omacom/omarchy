[Unit]
# Broken or unresponsive TPM hardware (e.g. some ThinkPads) makes PCR extend
# fail even with --graceful. Upstream FailureAction=reboot-force then loops
# the machine through login. Prefer continuing without the measurement.
FailureAction=none
