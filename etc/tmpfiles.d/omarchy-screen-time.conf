# Root's, and root's only: the config holds the parent PIN hash and the state
# holds the days the daemon is enforcing. The daemon's unit creates the runtime
# directory under /run itself.
d /etc/omarchy-screen-time 0700 root root -
d /var/lib/omarchy-screen-time 0700 root root -
