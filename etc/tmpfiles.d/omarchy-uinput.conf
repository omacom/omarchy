# Steam Input creates virtual gamepads through uinput. The kernel's static node
# can miss udev's uaccess rule during early boot, so enforce access after /dev
# has been populated.
z /dev/uinput 0660 root input - -
