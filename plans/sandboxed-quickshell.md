# Ward: sandboxed Quickshell plugins

Scope: Ward protects third-party plugins published through the Omarchy plugin registry. Omarchy's first-party plugins stay in-process. Users may also explicitly install their own plugins as trusted in-process code; Ward must not remove that path. Registry installation must select the isolated path independently of a plugin's self-description. The current manifest-selected preview is not yet registry-provenance enforcement. Ward itself, including its tests, must remain independent of concrete plugins; plugin compatibility experiments live outside its codebase. Sandboxing first-party plugins is not an acceptance target.

Status: Ward's admitted, sandboxed Quickshell worker now renders through the Rust controller and a native Qt item. The existing shell has an explicit native activation path and a first-party graphical reviewer over the command contract. Private-display tests cover review/approval/activation/revocation, GPU effects, keyboard and wheel/touchpad input, popups, integer-scale resizing, and brokered notifications; synthetic service tests cover selected MPRIS access. This remains a development-only, uninstalled preview; full demo parity and desktop acceptance are unfinished. Plugins declaring sandbox execution never enter the trusted QML loader. Existing third-party plugins without that declaration remain in-process and must still be treated as trusted code.

## Size constraint

Build the smallest system that preserves real Quickshell across process isolation. Prove one complete plugin before generalizing. Keep experiments outside the repository; do not land dependency forks, a replacement renderer, a provider catalog, or an extensible policy framework to make a demonstration work. Logical responsibilities do not require separate crates or services.

This POC is greenfield. Rewrite superseded sandbox assumptions and update their fixtures instead of adding legacy schema readers, aliases, migrations or runtime fallbacks. Existing unpermissioned v1 plugins remain explicitly trusted after user review, on the existing separate in-process path. Small implementation size must not force broad permissions: the grant model should express the narrowest enforceable resource and operation scopes needed by plugins.

## Product and demo target

Command-first: the existing `omarchy plugin add` command (also routed as `omarchy plugin install`) owns Git ingress and the checkout under `~/.config/omarchy/plugins/<id>`. Keep `update`, `validate`, `enable`, `disable`, `list`, and `remove` as the product workflow. Review and exact-revision approval extend that command family; a later graphical reviewer calls the same commands rather than implementing a second installer or permission authority.

The demo must reach the product fidelity of the previously reviewed PR #8956, not stop at isolated rendering fixtures. Its reviewed reference ports were GitHub, Radio Atlas, Omagotchi, and AirPods. Use their visible breadth as the comparison: useful service/network content, playback interaction, an animated desktop companion, and device/status controls. Exact provider-framework or restricted-worker architecture parity is not a requirement; preserving real Quickshell, native modules, and sandbox-local helpers is a differentiator of this implementation. Keep the comparison pinned to the reviewed revision `3422da98406a3fdd11b1a92bd19681116b27b531`, and do not copy its implementation.

The final compatibility proof has a stronger, explicit target: use the four actual [first plugin competition selections](https://omarchy.org/news/2026/08/the-first-plugin-competition-winners/) and preserve their exact original feature sets. Radio Atlas, Omagotchi, AirPods, and the GitHub honorable mention are not licenses to substitute smaller self-authored lookalikes or the older schema-v2 migration drafts. Pin each original baseline, inventory its features and integration points, then measure the changes required to run that source inside this sandbox. Keep unchanged source where possible; document every adapter, manifest, helper, or runtime change and every feature still not working.

The pinned original checkouts, feature checklist, source-level host dependencies and baseline test results are recorded in [the winner compatibility ledger](sandboxed-plugin-winners.md). None of those original-source tests is counted as sandboxed feature parity.

Plugin adaptations belong on dedicated review branches in `jacob-vincent-mink/omarchy-radio-atlas`, `jacob-vincent-mink/omagotchi`, `jacob-vincent-mink/omarchy-pods`, and `jacob-vincent-mink/omarchy-github`. Create review PRs only into those fork repositories. Do not push branches, open PRs, or make other writes to the original authors' repositories. The Omarchy runtime remains on its own review branch/PR into `jacob-vincent-mink/omarchy:quattro`.

When the ports are ready, the user has explicitly required and authorized a final E2E demonstration of all four on this machine's real Omarchy desktop. Private fixtures remain the preparation lane, not a substitute for that proof. Plan reversible activation and restoration of the existing desktop state before that session; preserve account data, pet state, device configuration, and unrelated processes. Record original versus sandboxed behavior feature by feature, including actual network content, playback, pet interactions, and device controls. A missing feature must remain an explicit gap rather than being removed from the success criteria. This final demonstration does not itself replace the separate deferred release/security acceptance gates.

Demonstrate one continuous operator flow: install through the real Omarchy command, inspect a named immutable revision and requested access, explicitly approve selected grants, enable it on the intended host surface, interact with useful live content, update and re-review without implicitly broadening authority, then disable/revoke and show the surface and helper processes disappear. Commands must work first; the UI must make the same flow understandable and polished. Show declined permissions, actionable startup errors, and a normal restart/reopen as product states, without resuming the deferred adversarial test program.

Track maintained runtime, glue/commands, and test source separately; exclude generated bindings, dependencies, lockfiles, and temporary experiments from authored-source counts. Compare process count, debug/release build context, idle memory, frame cadence, and startup behavior on the same representative demo before claiming an overhead advantage. Smaller code and infrastructure are metrics toward the full demo, not grounds for omitting visible functionality. Current private-fixture timings and memory figures below are preliminary, not an equivalent-demo result.

## Contract

The trusted shell owns real desktop windows, placement, input focus, dismissal, and surface lifetime. Each plugin runs real Quickshell against a private display inside its own sandbox. QML, JavaScript, bundled native modules, and helper processes execute there, never inside the trusted host.

A Rust controller owns plugin identity, grants, bounded requests, worker supervision, and revocation. Qt glue only presents surfaces and forwards host-authorized input. Plugins do not receive host QObjects, the real compositor socket, ambient session D-Bus, host command execution, or general host filesystem access.

Preserve these capabilities, subject to explicit resource grants:

| Capability | Required behavior |
| --- | --- |
| Custom visuals | QML/JS, animations, transparency, accelerated effects, and native QML modules |
| Surfaces | Embedded bar widgets, panels, popups, overlays/pets, multiple surfaces, and eventually replacement bars |
| Helpers | Bundled processes inherit the plugin sandbox and are terminated with it |
| Resources | Private storage, selected files, narrowly filtered services, and separately approved network access |
| Host effects | Narrow typed operations where direct resource grants cannot safely meet the need |
| Permissions | Requests are not grants; approval is tied to an exact revision and authority set; expansion requires new approval |

Compatibility is not permission to retain ambient authority. Existing shell object injection and service imports need adapters or explicitly unsupported behavior, not insecure fallback. Host arbitrary command execution is a separate full-authority decision, not an ordinary helper permission.

The permission contract requires required and optional requests, with a runtime-readable view of the actual grants so plugins can adapt to declined optional access. A missing required grant prevents activation with an actionable explanation, never becomes implicit approval. The current implementation exposes a read-only admitted-grant snapshot and supports explicit read-only/read-write directory selection, capped by 256 descriptors and a 64 KiB persisted-record budget. Approval explains that writes affect real host data and revocation cannot undo completed writes. Private-display reviewer tests exercise explicit writable selection and actual sandboxed writes to disposable host data. The maximum count has a persistence round-trip test, not yet an end-to-end maximum-count worker proof. Private persistent storage now uses an explicitly selected per-identity home directory; quota/disk budgets and original pet UI restart acceptance remain unfinished.

## Threat model

Assume plugin code, native modules, helpers, manifests, and protocol messages are malicious. Protect host files and services, other plugins, permission records, shell availability, and input outside the plugin's assigned region. Authenticate identity from the controller-created connection, not a plugin-supplied identifier. Bound messages, surfaces, buffer dimensions, rates, memory, and processes before accepting untrusted workloads.

The kernel, graphics driver, installed runtime, compositor libraries, controller, and presentation bridge remain trusted. A granted GPU render node exposes driver attack surface; it is not equivalent to CPU-only isolation. Transparent pixels are not automatically click-through, and alpha must not substitute for an explicit input region. Worker crashes, malformed input, and teardown must not take down the shell.

Prefer existing isolation and lifecycle mechanisms: Bubblewrap, systemd resource limits, filtered D-Bus connections, and portals where applicable. Do not create another implementation of mechanisms they already provide. Fail closed when required isolation cannot be established.

## Staged implementation

Product priority: adversarial testing is deferred at the user's request while the permission, enable/disable, and runtime-management surfaces are built. Before release, return to malformed Wayland clients, oversized requests/buffers, request floods, controller crash/hang containment, and isolation between simultaneously running plugins, then run disposable-VM acceptance. The exploratory raw-client fixture was removed rather than counted as coverage: its object-ID/binding handshake was not validated, so its apparent disconnections did not establish the intended containment results. Existing focused lifecycle and boundary tests remain, but they do not satisfy this broader release gate.

1. Prove private surface hosting with real Quickshell: animation, keyboard input, popup repositioning, transparency/input regions, accelerated effects, resize/scale, focus, worker death, and measured overhead. Compare Qt hosting with Smithay before selecting the smallest maintainable implementation.
2. Establish the minimal Rust controller and Qt bridge. Add connection-bound identity, versioned bounded IPC, disconnect behavior, and surface cleanup. These are logical boundaries, not a commitment to three crates.
3. Complete per-plugin filesystem/process isolation, private writable storage, resource limits, helper cleanup, and native-module packaging. Validate using hostile fixtures.
4. Add only the first grants: private storage, a selected read-only directory, one filtered service connection, and one brokered operation. Specify crash-safe publication and recovery before selecting persistence; atomic single-writer JSON is a candidate, not a settled design.
5. Test revocation during activity and persistence failures. Close admission first, invalidate stale requests and replies, withdraw surfaces, and terminate the worker tree when a directly granted resource must be removed. Revocation cannot undo completed writes or erase data already read.
6. Port representative plugins, starting with an animated widget/keyboard panel and a helper, then an overlay/pet, service/network integration, and a replacement bar. Expand APIs only in response to demonstrated need.
7. Harden an opt-in preview: protocol fuzzing, persistence fault injection, resource exhaustion, and graphical acceptance tests in a disposable environment. Verify scaling, multiple monitors, suspend/resume, and recovery before enabling it.

Do not build the permission framework before the surface-hosting exit gate passes.

## Feasibility findings

Temporary tests on Quickshell 0.3.1, Qt 6.11.2, Mesa 26.2.1, and Intel Arc B390 established the following. These are self-authored happy-path fixtures, not a security assessment or a compatibility guarantee.

| Test | Observation |
| --- | --- |
| Qt host, software buffers | Real `PanelWindow` and `FloatingWindow` rendered; synthetic private-display input reached QML text and click handlers |
| Qt host, accelerated buffers | Rendered on the GPU only after temporarily adding the missing compositor DMABUF integration and device feedback; no patched dependencies were installed |
| Qt host, popup | The installed XDG shell v1 support triggered Quickshell's hide/reopen reposition fallback and the popup immediately closed |
| Qt host, shutdown | Initial client-buffer teardown crashed; explicitly disconnecting private clients before host shutdown avoided the crash in subsequent short tests |
| Smithay 0.7, headless | Unpatched Smithay accepted real Quickshell GPU buffers; the captured frame showed the panel, animated widget with shadow, edited text, and open popup |
| Smithay popup/input | Private synthetic pointer/keyboard events reached QML; popup reposition was acknowledged and the popup remained present at capture |
| Helper | A worker-spawned helper ran inside Bubblewrap and confirmed the real home directory and session-bus path were absent |
| Qt GPU bridge | A small native Qt item imported two Rust-composited DMABUFs through EGL and Qt's public texture interface, without CPU pixel transport; rendering and input passed with Qt's threaded render loop |
| Input regions | The bridge received the worker's input region; a click in a subtracted region reached a host-owned item underneath, not the worker's click handler |
| High DPI and resizing | At integer scale 2, rendering remained correct, the worker window accepted a change from 480×280 to 620×300, and input remained correctly mapped after resizing the host item |
| Native module | A bundled native QML module loaded inside the sandbox after using a relative library RUNPATH; it confirmed the host home and session-bus paths were absent |
| Host dismissal and teardown | A host click dismissed the private popup and withdrew keyboard focus; producer exit removed the Qt scene-graph content, verified in a captured frame |

Smithay resolves the two protocol gaps encountered in the Qt experiment and now has a working headless Qt embedding proof. The two-buffer prototype uses conservative GPU waits and a frame acknowledgement: the currently displayed buffer remains unchanged while the next buffer is rendered. Readback is used only for test capture, not presentation. Its hardcoded geometry, synchronous test transport, fatal error handling, and incomplete surface policy must not be promoted into the trusted shell.

A short, sequential 800×480 comparison measured median frame intervals of 16.66 ms for both the direct fixture and the bridge. The p95 interval was 16.88 ms direct and 17.40 ms through the bridge. The direct Qt fixture used 113,568 KiB proportional memory; the bridge's Qt host, worker, and inner Rust compositor together used approximately 209,839 KiB, about 94 MiB more. These are single-run, debug-build fixture measurements, not a production benchmark, input-latency measurement, or full GPU-memory accounting.

Real desktop focus/click-through, fractional scaling, multiple monitors, dynamic output changes, complete popup grabs, sustained resource behavior, and adversarial crash containment remain unverified. The headless evidence is sufficient to begin the minimal runtime implementation, not to enable it for user plugins or claim the shipping validation gate has passed.

## Worker restriction implementation

### Revision and publication rules

Approval names a content-addressed snapshot, not the live checkout. Snapshotting copies bounded regular files/directories without following links, preserves executable intent, excludes Git administrative data, and hashes the copied bytes and relative paths. The initial limits are 64 MiB, 4,096 entries, and 16 directory levels. Symlinks, hardlinked files, and special files are rejected; bundles must currently contain their actual helper/native-module files. The requested authority comes from that copied manifest. Source updates cannot silently replace an approved revision; modified snapshots fail verification.

Grant publication uses one controller-owned JSON record per plugin, a stable writer lock, and a durable pending marker. Write and synchronize the pending marker before replacing a record; synchronize the new record and parent directory before removing and synchronizing away the marker. Readers reject pending, malformed, missing, or mismatched state. Explicit recovery disables authority instead of completing a pending approval. Marker removal is the visibility commit point: an interrupted operation after that point may have committed exactly the approved record even if its caller received no acknowledgement; before it, admission remains closed. Tests inject errors and abrupt process exits at seven publication checkpoints.

Revocation closes admission before stopping the recorded service and verifies descendant exit before reporting success. A replacement service identity is recorded before launch, and the previous service is stopped before publishing the replacement. The controller rechecks its actual service identity, epoch, revision, and approval before admission; it subsequently checks authority during its event loop. The persisted unit identity permits cleanup after the launching shell disappears. Tests cover stale epochs, rejected same-user impostors, active-controller revocation, interrupted publication during operation, malformed state, writer contention, and explicit recovery. The presentation and notification integration tests below additionally cover surface withdrawal and in-flight helper cleanup.

The preview manifest adds `sandbox: { version: 1, requests: { ... } }` alongside the existing plugin metadata. Omitting `sandbox.entryPoint` selects the shared worker loader for the original manifest's `entryPoints`; an explicit `entryPoint: "worker.qml"` retains the custom worker path. Requests and grants are separate; every authority defaults to denied. Grant records describe named directory access, optional broad networking or named mediated HTTP scopes, one exact MPRIS service, notifications, browser/webapp navigation, exact own inline-settings keys, and private storage. Required requests must be selected before activation; optional requests can be declined and the admitted snapshot reports actual access to the worker. Directory/network/HTTP/media/notification/navigation/settings/storage bindings are connected to the worker launcher. Storage selects a persistent per-identity filesystem mounted at the private home, separate from inline shell-entry settings. Revocation retains data but removes worker access. The [granular grant inventory](sandboxed-plugin-grants.md) records current scope syntax, bounds and unfinished integrations.

The shared runtime supports one `bar-widget` and optional `service`/`overlay`, requiring exact, unique kind/entry-point matches at review. Omarchy separately stages its host-owned `worker.qml`, shared `qs.Ui`/`qs.Commons`, and `PluginShellApi`; Ward's CMake build installs only the native executable/module. The host opens an explicit runtime directory and sends its descriptor to the controller for a read-only `/runtime` mount; Omarchy's sandbox-local bootstrap sets `OMARCHY_PATH=/runtime`. There is no adjacent-runtime lookup. The worker does not receive the host repository, configuration or service tree. These host-versioned libraries execute inside the existing sandbox, not in the desktop shell, and do not replace exact-revision approval of plugin bytes. Directory identity is pinned, but host packagers must keep its contents unchanged while selected. Missing runtime files and QML load failures do not fall back to the trusted loader. See the [runtime contract](../docs/ward-runtime.md).

The trusted host supplies a detached own-entry settings object (without structural `id`/`sandbox` keys), foundational palette, merged shell tokens, radius, gap and resolved font-family name. A one-way context record on the existing admitted connection carries exactly one sealed, at-most-64-KiB JSON descriptor. Other records still reject descriptors; reverse-direction contexts are rejected. The initial snapshot precedes configuration/worker loading; subsequent snapshots coalesce within each dispatch and atomically replace `/context/state.json` in a pinned read-only directory. Worker-local `Color`/`Style` use those values without local theme/font/compositor probes overwriting them. Neither host QObjects nor writable configuration are exposed, and ordinary trusted-shell theme loading is unchanged. Private user font files are not mounted by this contract.

The loader retains its widget, own service and own overlay across context updates, with local lifecycle/bar facades but no host command executor. The existing CMake-staged test covers initial/live settings and theme delivery through the production host adapter, detached nested settings larger than a control packet, font/width/colour changes, retained service state, scoped lookup, panel/Escape cycles, click-through and revocation. The original GitHub experiment also receives live palette/font updates with its search text and two-Escape behavior preserved; captures were visually inspected. Initial bar placement still follows the manifest's default section, not host slots. Tooltip forwarding, cross-panel handoffs, service-only readiness, authentication and full desktop parity remain gaps.

Explicit settings read/write key sets and `--read-setting` / `--write-setting` selections scope the `bar.shell.updateEntryInline(ownId, settings)` callback. The shared runtime coalesces saves over 300 ms and keeps one subprocess in flight. It reuses the sealed context encoding on the private request broker, not on the main controller channel; reverse contexts on that main channel remain forbidden. The broker reuses notification admission, kernel-unit authentication, bounded helper delivery and revocation checks, with separate operation grants, delivery slots and budgets (settings: burst eight, refill one per second). Neither socket alias bypasses the other operation's grant. The broker supplies the admitted identity to the fixed `omarchy-plugin-settings-apply` helper and rejects identity changes and structural keys. Rust filters initial/live setting reads and validates each written key. The host checks the active sandbox entry, preserves its identity/marker and unselected settings, and merges only the approved patch through its existing configuration writer. Same-id legacy bar entries remain untouched. No worker-selected command, host QObject or writable configuration file crosses the boundary. A success reply means the host accepted the update, not a new disk-durability guarantee beyond the existing asynchronous shell writer. Rejection restores the latest detached host settings and displays a passive, click-through error notice. Private tests cover granted/denied saves, reconciliation, persistence across worker restart, combined grants, malformed/structural requests, cross-alias denial, timeout and in-flight revocation; the reviewer tests cover explicit selection and reopening with an empty draft. Before the key-scope rewrite, an uncommitted experiment verified all six settings-page controls in the unchanged original GitHub plugin: real input, host-written values, page reopening, private-host restart and visible denied-save rollback. The [winner ledger](sandboxed-plugin-winners.md#github) records the exact scope; authenticated filtering and browser opening remain unverified.

### Implemented boundary primitives

`native/ward` is one Rust crate. Its worker bootstrap applies Landlock and a seccomp filter after Bubblewrap establishes isolation, before untrusted code executes. It is not a complete plugin controller by itself.

Read-only bind mounts do not make Unix sockets read-only: connecting to a socket beneath a directory grant could otherwise confer extra host authority. The initial runtime therefore requires Landlock ABI 9 and explicitly permits private controller sockets. Host directory descriptors are rejected as IPC grants. Separately, the launcher-created private tmpfs directories permit local sockets so helpers can communicate with each other; symlinking from those directories to an ungranted host socket remains denied. Abstract Unix connections and signals are scoped; namespace creation and selected dangerous kernel interfaces are denied, while ordinary threads, processes, and helper execution remain available. Failure to install any required restriction is fatal to worker startup; there is no weaker fallback.

Unit tests check the filter's architecture, syscall, namespace, and socket-family decisions. Fresh-process kernel tests verify allowed/denied sockets, rejection of a directory as IPC authority, ordinary thread creation, and helper inheritance. The actual Rust restriction module was also linked into the temporary headless Quickshell test: GPU rendering, native modules, helpers, high-DPI rendering, resized input, and host-owned dismissal continued to work. Kernel-dependent tests explicitly report a skip after verifying fail-closed behavior on unsupported systems.

The trusted per-plugin controller runs in a generated transient user service with memory, swap, process, file-descriptor, and CPU ceilings. Both launcher and controller verify kernel cgroup settings; the controller checks before starting untrusted code. A watchdog is refreshed from the healthy controller event loop, not an independent thread. Stop/revocation checks kernel cgroup population rather than trusting a successful manager reply. Kernel tests verify explicit stop, controller crash, controller hang, process exhaustion, memory exhaustion, and host-channel EOF, including descendant cleanup. The tests do not establish GPU-memory ceilings.

The private host/controller channel uses non-blocking sequenced Unix records, capped at 4,096 bytes and four descriptors, with bounded kernel send queues and no internal unbounded queue. Descriptor transfers are close-on-exec and rejected/truncated records close their received descriptors. Admission checks the peer's UID, kernel-provided socket pidfd, and membership in the specific launched service. Versioned control records have a bounded dispatch/rate budget and a three-second host lease. The real controller executable passes stop, disconnect, silent-host, replay, malformed-record, and unexpected-message tests. This is not the worker's Wayland endpoint or an arbitrary host-command RPC.

The initial Bubblewrap profile strictly separates namespaces, clears the environment, mounts descriptor-pinned bundle/bootstrap/private-socket inodes, drops capabilities, and closes inherited descriptors before applying worker restrictions. Named directory grants appear under `/grants/<slot>` with selected read-only or read-write access and retain the selected directory's device/inode identity. Write-only access is rejected. Network access remains isolated unless broadly granted; that broad grant includes the host network namespace, including loopback, plus resolver and certificate configuration. Named mediated HTTP grants instead retain worker network isolation and route validated requests through the trusted Rust broker. Tests use temporary loopback listeners, not external services or real credentials. Graphics workers receive only the selected render node and its device-specific sysfs subtree, not a modesetting node or all host devices. Persistent storage is an explicit home-directory bind; it grants no host Unix-socket access, and ungranted workers receive an ephemeral home.

Supervised worker tests verify read-only files, pathname replacement resistance, absent host paths/environment, denied and granted network access, denied namespace creation, socket confinement, private helper IPC, inherited-descriptor cleanup, and temporary-space exhaustion. Selecting a host directory does not authorize its sockets, even through a symlink from private writable space.

### Selected media service

An approved media grant launches `xdg-dbus-proxy` inside the same resource-limited controller service. The worker receives only its descriptor-pinned filtered socket at `/run/plugin/media`; the real session-bus socket remains absent and inaccessible, including through a read-only directory grant. The proxy is constructed from the admitted grant, not from worker-supplied rules, and the launcher rejects a prepared proxy whose selected name does not match that grant. Missing proxy support, readiness failure, or proxy exit fails closed. The controller waits at most one second for proxy readiness before starting plugin code.

Rules expose one exact `org.mpris.MediaPlayer2.*` name chosen by the approver, without a wildcard grant. Calls are restricted to `/org/mpris/MediaPlayer2`: property `Get`/`GetAll`, introspection, and `Play`, `Pause`, `PlayPause`, `Stop`, `Next`, `Previous`, `Seek`, and `SetPosition`. Property-change and seek signals are admitted from that endpoint. `OpenUri`, application quit/raise, property writes, name ownership, monitoring, other interfaces/methods, and other object paths are not granted. Property reads apply to properties on that selected object; the proxy does not inspect the requested interface argument inside `Get`/`GetAll`. Volume, rate, shuffle, and loop-state setters are therefore currently unsupported even when a player advertises them.

This is a filtered D-Bus connection, not an exact-name packet router or a rate-limited method broker. Upstream filtering associates rules with unique owners and may retain them after a well-known name is released. Explicit revocation kills the entire proxy/worker service and closes established connections; replacing a selected owner is not itself a permission-revocation operation. These semantics, and the trusted selected application's behavior, are part of the resource contract. [Upstream proxy policy](https://github.com/flatpak/xdg-dbus-proxy/blob/main/xdg-dbus-proxy.xml)

Tests create a private bus with no activation directories and two fake players; no active media application is contacted. The sandboxed worker can read properties and control the selected player, but cannot call forbidden methods, access an unrelated alias or player, bypass rules through unique names, own a service name, monitor the bus, or connect to the real test bus through a directory grant/symlink. Proxy shutdown closes an already-connected socket. The unrelated fake player survives. An earlier first-party media-widget port was only an experiment; it has been removed from Ward's tests because first-party plugins remain in-process.

### Text-only notification broker

An approved notification grant adds one descriptor-pinned private socket at `/run/plugin/notify`. Quickshell can invoke `Process { command: ["/bootstrap", "--notify", title, body] }`; the helper inherits the worker's restrictions and connects only to that fixed endpoint. It does not expose an arbitrary method, executable, desktop action, replacement identifier, image, application identity, or bus connection. Requests are versioned, descriptor-free JSON records with exactly `version`, `title`, and `body`. Titles are limited to 160 UTF-8 bytes and bodies to 2,048 bytes; control characters and bidirectional override/isolation marks are rejected, except body newlines.

The Rust controller authenticates each peer's UID, socket pidfd and membership in its own service, then checks the current recorded unit, epoch and notification grant under the publication lock before starting delivery and before acknowledging completion. Identity is derived from that record. Delivery invokes the fixed `$OMARCHY_PATH/bin/omarchy-notification-send` helper with low urgency, a five-second expiry, a plugin-labelled title/application name, and escaped body markup. Both text positionals carry trusted prefixes because the existing helper recognizes option flags in those positions. No worker text is interpreted as shell code or a notification option.

The broker accepts at most 32 connections per second, authenticates at most four per dispatch, retains at most four waiting clients for one second, and permits one in-flight delivery with no effect queue. A two-token per-controller budget replenishes one token every 30 seconds. The delivery helper runs under a one-second process-group timeout in the same bounded service; the event loop polls completion without waiting synchronously. Requests receive `delivered`, `invalid`, `busy`, `rate_limited`, or `failed`; the worker helper waits at most three seconds. An explicit host relaunch starts a new budget; there is no automatic worker restart policy.

Revocation closes new admission, kills in-flight helper descendants with the whole service, and prevents a stale success acknowledgement. It cannot undo a notification already accepted by the desktop service; sent notifications are not withdrawn, and delivery has no transactional cancellation or exactly-once guarantee. `delivered` means the existing helper returned success, not proof that a user saw the notification.

Tests use a private recorder, never the active notification service. They verify denied grants, rejected same-user outsiders, malformed/identity-bearing/descriptor-bearing requests, option and body-markup isolation, busy/rate/timeout behavior, and revocation during delivery. A parser test runs the real Omarchy notification helper against a recorder-only `busctl` stub and checks every typed argument, including absent actions and image hints. A separate admitted real-Quickshell test completes two native `Process` requests through the restricted helper.

### Integrated presentation

The optional `graphics` feature runs an unmodified Smithay private compositor in the admitted, resource-limited controller. It starts real Quickshell only after the host configures a viewport. The initial compositor accepts toplevels, layer surfaces, and popups, with limits on clients, surfaces, input rectangles, and buffer dimensions. Native modules can be bundled under `native/` in the approved snapshot. GPU rendering, native-module imports, and helper execution do not grant access to the real compositor or session bus.

Inside the assigned canvas, layer surfaces stack as background, bottom, ordinary windows, top, and overlay; later surfaces within a layer appear above earlier ones. Committed layer changes affect rendering and hit testing together. Anchored margins inset stretched dimensions, explicit sizes between opposite anchors remain centered, and arithmetic bounds extreme margins and sizes to the private viewport policy. Popups stay directly above their own parent tree, below higher layers. Their positions use committed XDG geometry and explicit window-geometry offsets; bounded positioners use Smithay's flip/slide/resize constraints against the assigned canvas. A positioner that requests no adjustment can remain clipped outside it.

These are private composition rules, not permission to choose a real desktop layer or reserve screen space. Plugin exclusive zones do not reserve space on either the real desktop or the private canvas. The trusted shell must explicitly choose any actual edge, overlay, replacement-bar placement, and reserved area. Keyboard-none layer clicks preserve existing private keyboard focus; other layer policies can receive focus only after host-authorized input, never by demanding an exclusive desktop grab. Complete popup-grab semantics and implicit client-decoration geometry remain unfinished.

A separate real-Quickshell fixture deliberately creates an animated masked overlay before its inset bar and creates the background last. It checks stacking independent of creation order, moving the overlay to the bottom layer, matching click targeting, text focus retained across a keyboard-none click, input-mask holes, a fully visible edge popup, acknowledged reposition to the opposite edge, input at the committed new position, and host dismissal. Its captured frame sequence was inspected for animation, bar insets, intentional layer overlap, popup text fitting at both edges, and final withdrawal. This is a private overlay/bar composition proof, not an installed replacement bar or live-desktop acceptance test.

Two controller-owned, linear ARGB buffers carry completed output to Qt. Untrusted worker buffers and fences are not forwarded to the host. Versioned presentation records validate dimensions, stride, format, descriptor counts, mask bounds, generation, serial, and buffer ownership. The producer never writes the displayed slot, and cannot advance until the host acknowledges the next frame. Its GPU wait occurs in the supervised controller; Qt inserts a completion fence and polls it with zero timeout, then acknowledges through Rust. A stuck controller stops refreshing its watchdog. This does not protect against a kernel/driver failure or establish GPU-memory quotas.

The optional `qt-bridge` feature uses CXX to keep decoding, admission, launch, leases, and service cleanup in Rust. The Qt QML module exposes `PluginView` with explicit host-selected store, plugin identity, controller executable, and viewport. It cannot approve revisions or broaden grants. Startup and cleanup run on a background thread, with 64 input commands and eight presentation updates of queue capacity; overload, malformed records, EOF, or lease expiry terminate that session. Dropping the host item does not synchronously wait for the service manager. An error clears its input mask and scene-graph content without terminating the host.

Input masks compose each mapped surface's independent input region, including subsurfaces, clipped to its own view and the assigned viewport. The item takes keyboard focus after a host pointer press or an explicit host summon. Keys, including Escape press and release, go to the focused worker control so its existing clear-search, menu and panel-close behavior can run. Focus loss and explicit host dismissal separately clear private keyboard focus, cancel pressed input/grabs, and dismiss popups. The shared runtime now carries numbered own-panel open/close requests and bounded panel-state replies, so summon/hide/toggle leave the persistent widget mapped. Host focus loss, workspace/monitor signals, and existing bar popup ownership drive dismissal; worker-reported panel state cannot itself acquire host focus or popup ownership. The original GitHub UI passed private-display focus/outside-click dismissal, IPC summon with immediate typing, hide, reopening, and rapid-toggle checks. Workspace/monitor handlers were exercised through direct signal emission, not real Hyprland workspace changes. Full desktop policy, fractional host scaling, multiple outputs, complete popup grabs, and scene-graph/context-loss stress testing remain unfinished.

After host pointer activation, the private compositor honors the topmost mapped top/overlay layer's exclusive keyboard request. This lets the original KeyboardPanel acquire focus when opened from a keyboard-disabled bar icon, then retain it when its brief exclusive prime becomes on-demand. Unmapping or switching to keyboard-none removes that focus. Host dismissal and resizing disarm automatic private focus until another host pointer press; an exclusive layer left running cannot rearm it. The private `focus` test covers the prime, actual key delivery, persistent exclusive mode, dismissal, reactivation, keyboard-none and unmapping, including continued client frames and rejected keys during the unfocused intervals. This is private-display behavior, not proof of real-desktop focus or complete popup grabs.

Scrolling uses its own 28-byte control record with no descriptors: bounded position, horizontal/vertical deltas, and an exact wheel/finger/end source. Rust validates both encoding and decoding, rejects unknown sources, invalid zero/end combinations, and magnitudes outside ±4096, then re-hit-tests within the current viewport without acquiring keyboard focus. Wheel deltas preserve partial detents through Wayland `axis_value120`; finger deltas are logical pixels and end records stop both axes. The Qt item respects its input mask and resize pause, maps pixel deltas into the private canvas, and preserves the already-applied natural-scroll direction. The Qt integration test verifies horizontal/vertical wheel input, a half-detent, finger pixels, and gesture completion after moving and scaling the item. [Qt wheel-event semantics](https://doc.qt.io/qt-6/qwheelevent.html)

Quickshell's standard output is redirected into the controller's existing bounded worker-error pipe, so configuration failures are diagnosable without adding an unbounded log queue or a worker-selected host log destination.

The host explicitly calls `PluginView.configure(logicalWidth, logicalHeight, scale)` to change that canvas without restarting the worker or changing its approval. Changing the Qt item's own rectangle alone still scales its existing canvas. The Rust session retains one in-flight resize and coalesces later requests to the latest requested geometry. Each accepted configuration announces its logical viewport and strictly increasing generation before describing two new output buffers; unsolicited generations, geometry mismatches, mixed-generation buffers/masks, and stale acknowledgements fail closed. A new generation cannot overtake an unacknowledged old frame, and a subsequent resize waits for the first new frame to be presented. A coalesced request that returns to the current dimensions still receives a fresh generation, so input cannot remain paused waiting for a nonexistent acknowledgement.

Qt keeps its old image while replacement buffers arrive, then replaces the imported scene-graph node on the first new frame. It exposes `resizing` and suppresses private input until a newer generation matching the requested geometry has actually completed presentation. Resizing cancels private keyboard/button state and popups; later interaction must reacquire focus. The private output updates its mode and integer scale, reconfigures anchored layer surfaces, and explicitly sends per-surface preferred buffer scales through Smithay's compositor-v6 support, including newly created or subsequently committed surfaces. Output-scale announcements alone did not correctly update all worker buffers in the tested Qt configuration; the real scale-transition test exposed and verified this requirement. Old modes are removed from the controller's own mode list to keep that state bounded. [Smithay output API](https://docs.rs/smithay/0.7.0/smithay/output/struct.Output.html)

The Qt integration test hosts its trusted Quickshell window on a second private display. Pointer focus, subsequent typing, and popup clicks traverse Qt and the admitted controller into the sandboxed worker. It moves/shrinks the host item, resizes the private viewport from 800×480 at scale 1 to 640×400 at scale 2, and checks an edge click and further typing against the new coordinate mapping. A second burst returns to the same dimensions and must restore input. GPU readback checks the worker's retained text, responsive panel, popup dismissal, and interaction indicators; after durable revocation it checks that plugin pixels disappear while host-owned animation remains. Before/after captures and a short test-frame sequence were visually inspected. These tests never inject input into the active desktop. They are not substitutes for the disposable-VM graphical acceptance gate.

### Development verification

The first product commands reuse `omarchy plugin add` / `install` for ingress. `omarchy plugin review <id> [--json]` finds the installed checkout through the existing catalog, publishes an immutable snapshot without executing it, and returns the revision and requested resources. `omarchy plugin approve <id> --revision <sha256>` passes explicit selections to the Rust store; neither the shell nor a future GUI writes grant records. Read-only folders, one exact media service, network, and text notifications are opt-in flags. Confirmation requires a terminal unless `--yes` is explicit. Approval is separate from activation. `omarchy plugin enable <id>` now calls the existing shell IPC, creates a trusted native surface, and waits for an actual mapped worker frame to finish presentation. The existing `disable` command routes declared sandbox plugins to durable revocation, independently of shell availability.

The hidden `omarchy-ward-runtime` helper exchanges bounded JSON with the native `--manage` mode and reports structured failures. Its default payload path is `$OMARCHY_PATH/lib/omarchy-ward`; no payload is installed by these changes. Set `OMARCHY_WARD_HOST` to an explicitly built binary and `OMARCHY_WARD_STORE` to a private development store when exercising the preview. The ordinary registry never returns an in-process QML URL for a manifest declaring `sandbox`, including when native support is unavailable. Existing trusted plugins without that declaration retain their current behavior.

`omarchy plugin list` now distinguishes approval from live activation; a stored unit identity is not treated as proof of a running session. Sandbox installation, update, and removal do not require a running shell. Update leaves the approved snapshot and grants unchanged and requests re-review; an update that removes the sandbox declaration is refused. Explicit removal stops the controller and purges the checkout, private saved data, permission records, snapshots, isolation identity and provenance; disable/revoke retains these while withdrawing authority. Removal failure retains denial and remaining ownership for retry, and snapshot ownership plus the writer lock prevent cleanup from affecting another identity. Staged reviews use ephemeral snapshots and leave no persistent security history before publication. Disable is idempotent for a never-approved plugin. The sandbox declaration is tracked independently of its value so a malformed declaration cannot fall through to the legacy command path.

`OMARCHY_TEST_WARD_HOST=/absolute/path/to/omarchy-ward bash test/shell.d/plugin-review-test.sh` tests the real add/review/approve/list/update/disable/remove commands with a disposable local Git repository and a stubbed shell endpoint. It verifies that review grants nothing, only selected access is approved, an actual Git update yields a new digest without replacing old approval, reapproval does not implicitly retain old grants, and disable/removal close admission even without the shell. `OMARCHY_TEST_HEADLESS=1 bash test/shell.d/plugin-registry-contract-test.sh` runs the actual registry in offscreen Quickshell with a private runtime directory, without connecting to the live desktop. These are normal product-flow checks; the broader adversarial program remains deferred.

At this command milestone, the native source tree contains 6,889 lines including inline Rust unit tests, plus 3,302 lines of separate native test/fixture source; the three new shell command files total 143 lines. This is a rough authored-source inventory, not a production-only LOC count or equivalent-demo comparison. It excludes dependencies, lockfiles, generated bindings, documentation, and temporary experiments. Recalculate and separate embedded tests before using it for a fidelity-matched size claim.

The activation milestone adds `SandboxedPlugins` as a trusted adapter in the existing shell, with an optional host-owned native component loaded only on explicit activation or a saved sandbox config entry. It never evaluates plugin paths. The real host window is a non-exclusive overlay-layer canvas on the largest logical screen, with a bounded input region derived from Rust presentation records. Empty areas pass through to other windows. The registry preserves a `sandbox: true` config marker across source changes, so replacing a checkout cannot turn that saved entry into in-process QML. Shared bar placement now uses host-owned slots; service-only readiness and complete desktop focus/output policy remain unfinished.

The first-party `omarchy.plugin-review` panel is available through `omarchy plugin review <id> --ui` and Setup › Plugins. It reuses the shell's KeyboardPanel, controls, theme tokens, scrolling and dismissal. Its draft is separate from saved grants: every request starts denied, approval names the reviewed digest, and activation requires another click. Commands remain the only mutation path. The graphical variant of `test/shell.d/fixtures/ward-integration/tests/activation.rs` uses real pointer input, scrolling and text entry to reject a relative folder, correct it to denied, approve selected access, separately enable, interact with the worker, reopen without changing grants, and revoke through the UI. It waits for scroll motion and focus to settle before choosing input coordinates. Selection, typing/error, approved and enabled captures were visually inspected at 800×480. Set `OMARCHY_TEST_REVIEW_CAPTURE=/private/path/review.ppm` to save these named state captures alongside that path. This is normal product-flow verification, not the deferred adversarial gate or live-desktop acceptance. The reviewer remains trusted in-process UI; only the synthetic plugin it approves runs in Ward.

That shell integration suite exercises the production review/approve/enable/disable commands and trusted surface adapter on a private display. It checks a real worker button, click-through to a separate underlying host window, command completion, and withdrawal without killing the host. The same test passes against a CMake-staged binary and QML module. Its captured frame was inspected for text, geometry, and the two independent interaction indicators. This does not exercise the complete stock shell root or live desktop. Activation and Qt resize/input tests share `native/ward/tests/support/desktop.rs` for private display setup, host cleanup, GPU readback, and frame acknowledgement. Product-specific integration lives outside Ward, which has no concrete plugin dependencies even in tests.

The bar-slot milestone adds one bounded worker-size record and a typed host allocation in the existing context. A host-owned spacer receives the size and publishes its slot position; neither plugin QML nor its inline settings enter legacy bar dispatch. The generic shared-worker test verifies every bar orientation, live resizing/movement, hide/reveal, clicks, settings, restart and revocation. `test/shell.d/fixtures/ward-integration/tests/bar.rs` additionally runs the complete shell root with two isolated widgets and a trusted in-process widget: both native panels hand off correctly, a slot moves, a trusted replacement bar preserves all widgets, and disabling removes only the native entries. Captures were visually inspected. That test exposed and fixed catalogue-component ownership across bar replacement; temporary diagnostics were removed. These are private-display results, not completed original-plugin or active-desktop acceptance.

Normal Qt layer-window teardown exposed Smithay 0.7's stale role-size validator ([upstream report](https://github.com/Smithay/smithay/pull/2071)). A narrow compatibility hook supplies inert dimensions only after a layer role is destroyed and resets them before any replacement-role requests; live-role validation remains unchanged. Destroyed roles also leave composition immediately. Remove this hook when the dependency contains the upstream fix; it avoids vendoring or modifying the dependency cache.

Run `bash test/shell.d/ward-test.sh` or `cargo test --locked --manifest-path native/ward/Cargo.toml`. Cargo is an optional development dependency; the shell test runner explicitly skips this native suite if Cargo is absent. Set `OMARCHY_TEST_SYSTEMD=1` to additionally run the temporary user-service lifecycle, controller, and sandbox-launch tests. Add `--features graphics` and `OMARCHY_TEST_GRAPHICS=1` for admitted headless GPU rendering. These opt-ins create short-lived, bounded test services, not desktop windows.

Build the development Qt module without installing it:

```bash
cargo build --locked --manifest-path native/ward/Cargo.toml --features graphics,qt-bridge
cmake -S native/ward/qt -B /tmp/omarchy-plugin-qt -G Ninja -DRUST_TARGET_DIR="$PWD/target"
cmake --build /tmp/omarchy-plugin-qt
OMARCHY_TEST_SYSTEMD=1 OMARCHY_TEST_GRAPHICS=1 OMARCHY_TEST_QT_BRIDGE=/tmp/omarchy-plugin-qt cargo test --locked --manifest-path native/ward/Cargo.toml --features graphics,qt-bridge --test qt
stage_root=$(mktemp -d /tmp/omarchy-ward-adapter.XXXXXX)
bin/omarchy-ward-stage-runtime "$stage_root/runtime"
OMARCHY_TEST_SYSTEMD=1 OMARCHY_TEST_GRAPHICS=1 OMARCHY_TEST_QT_BRIDGE=/tmp/omarchy-plugin-qt OMARCHY_TEST_WARD_HOST="$PWD/target/debug/omarchy-ward" OMARCHY_TEST_WARD_RUNTIME="$stage_root/runtime" bash test/shell.d/ward-integration-test.sh
```

For installed-layout verification, stage with `cmake --install /tmp/omarchy-plugin-qt --prefix <private-staging-directory>` and repeat the tests with `OMARCHY_TEST_QT_BRIDGE=<private-staging-directory>/lib/qml`; `OMARCHY_TEST_WARD_HOST=<private-staging-directory>/lib/omarchy-ward` selects the staged executable for activation. Stage the adapter separately and select it with `OMARCHY_TEST_WARD_RUNTIME`. The integration crate's `shared_runtime` and `shared_outputs` tests exercise Omarchy's original-entry-point loader and output ownership; the former also checks the staged loader matches source. Ward's native `streams` test has a separate non-Omarchy runtime fixture. `OMARCHY_TEST_ACTIVATION_CAPTURE` optionally selects the activation PPM capture, and `OMARCHY_TEST_SURFACE_FRAMES` selects an existing directory for shared-runtime captures. CMake accepts `-DRUST_PROFILE=release` for release builds; no default package or setup step installs these artifacts yet. See the [runtime staging reference](../docs/ward-runtime.md) for source selection and version-lifetime requirements.

Use an optimized build for live desktop trials: add `--release` to Cargo and configure a separate CMake build with `-DRUST_PROFILE=release -DCMAKE_BUILD_TYPE=Release`. Keep both `graphics,qt-bridge` features when building the executable and static library. Unoptimized executable hashing and rendering consume the controller's shared CPU budget and can cause concurrent host requests to time out; do not widen permissions or raise resource limits to compensate. Install that matched payload at the active development root only through an intentional, reversible session handoff, preserving user configuration and refusing restart while locked. Installing files does not change the compositor or user manager's existing `OMARCHY_PATH`. The checkout's generated `/lib/` payload is ignored by Git.

When using a separate Cargo target directory, pass that same absolute directory to CMake. `OMARCHY_TEST_CAPTURE` and `OMARCHY_TEST_QT_CAPTURE` optionally select explicit PPM capture paths for their respective tests. `OMARCHY_TEST_QT_RESIZE_CAPTURE` selects the verified post-resize frame; `OMARCHY_TEST_QT_FRAMES` selects an existing directory for a numbered PPM sequence of the short Qt test, useful for reviewing transitions. These are test readbacks, not the presentation transport or a frame-time benchmark. The Qt module requires a compatible OpenGL/EGL renderer; unsupported renderer/import paths report an item error rather than aborting the shell. Nothing automatically installs or imports the module into the running desktop.

Build the synthetic media service fixture with `cmake -S native/ward/tests/support -B /tmp/omarchy-media-fixture -G Ninja` and `cmake --build /tmp/omarchy-media-fixture`. Set `OMARCHY_TEST_MEDIA_PLAYER=/tmp/omarchy-media-fixture/media-player` with `OMARCHY_TEST_SYSTEMD=1` when running `--test media`. These tests check generic MPRIS grants and revocation, not a particular plugin or a port of the first-party media widget. The fixture requires Qt Core/DBus development libraries; tests exercise the installed `xdg-dbus-proxy` and `dbus-daemon`, without installing either or registering real session services.

Run `--test notification` with `OMARCHY_TEST_SYSTEMD=1` for the private notification broker tests; add `--features graphics` and `OMARCHY_TEST_GRAPHICS=1` for native Quickshell helper compatibility. These tests override `OMARCHY_PATH` only in their private controller wrapper, pointing to a recorder fixture; they do not change session-manager environment or send desktop notifications.

Run `--test surfaces --features graphics` with `OMARCHY_TEST_SYSTEMD=1 OMARCHY_TEST_GRAPHICS=1` for private layer, margin, focus, and popup-placement checks. `OMARCHY_TEST_SURFACE_FRAMES` optionally selects an existing directory for its short numbered PPM sequence.

The shared multi-output contract is implemented: one worker/service owns independent mixed-DPI presentation streams and per-placement widgets, while Omarchy owns panel placement, focus and roaming policy. Private-display tests cover shared state, same-output and cross-output ownership, hotplug, zero outputs and crash/restart. The [geometry handoff](fractional-scale-and-outputs.md) is historical; the current contracts live in the [architecture reference](../docs/ward-architecture.md) and [trusted-runtime reference](../docs/ward-runtime.md). Ward's native build and generic runtime fixture run without an enclosing Omarchy checkout, and Omarchy's separately staged adapter works against that same native build. Installed-VM and physical mixed-monitor acceptance remain unverified.

Next for the original demonstration goal: finish the four original plugins' outstanding live feature and lifecycle checks in the [compatibility ledger](sandboxed-plugin-winners.md), including hardware-dependent evidence. The former Radio approval blocker was resolved; incomplete acceptance is not completed parity. Broader host-effect separation, default packaging, provenance routing, disk budgets, installed-VM acceptance and further hostile-workload testing remain separately tracked release work in the [host integration plan](ward-host-integration.md). The storage filesystem contract must not be described as a mediated storage API.

Protocol references: [Qt XDG shell implementation](https://github.com/qt/qtwayland/blob/v6.11.2/src/compositor/extensions/qwaylandxdgshell.cpp), [Qt texture import](https://doc.qt.io/qt-6/qnativeinterface-qsgopengltexture.html), [Smithay DMABUF support](https://docs.rs/smithay/0.7.0/smithay/wayland/dmabuf/index.html), [Smithay XDG shell support](https://docs.rs/smithay/0.7.0/smithay/wayland/shell/xdg/index.html), and [Landlock's pathname socket restrictions](https://www.kernel.org/doc/html/latest/userspace-api/landlock.html).
