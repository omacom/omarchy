# Blackwell/B650I compatibility profile; see docs/blackwell-audio-compatibility.md.
# This increases latency for the whole graph; it is not a universal audio default.
context.properties = {
  default.clock.rate = 48000
  default.clock.quantum = 1024
  default.clock.min-quantum = 1024
  default.clock.max-quantum = 2048
}
