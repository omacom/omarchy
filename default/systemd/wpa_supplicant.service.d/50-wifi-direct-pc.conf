# Matches Arch Linux's stock D-Bus-activated wpa_supplicant service.
# On other distributions preserve the existing ExecStart flags and add -m.
[Service]
ExecStart=
ExecStart=/usr/bin/wpa_supplicant -u -s -O /run/wpa_supplicant -m /etc/wpa_supplicant/wifi-direct-pc.conf
