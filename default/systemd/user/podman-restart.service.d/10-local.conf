[Service]
# This login/logout service owns local containers, regardless of a developer's
# preferred remote endpoint for interactive Podman commands.
ExecStart=
ExecStart=/usr/bin/podman $LOGGING --remote=false start --all --filter should-start-on-boot=true
ExecStop=
ExecStop=/usr/bin/podman $LOGGING --remote=false stop --service --all
