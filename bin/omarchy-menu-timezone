#!/bin/bash

# omarchy:summary=Select and set the system timezone
# omarchy:group=menu
# omarchy:name=timezone

set -e

timezone=$(timedatectl list-timezones | omarchy-menu-select "$(omarchy-i18n "Set timezone")" -- --width 520 --maxheight 520) || exit 1
sudo timedatectl set-timezone "$timezone"
omarchy-shell -q omarchy.clock refresh
omarchy-notification-send "$(omarchy-i18n "Timezone is now set to %1" "Timezone is now set to %1" "$timezone")"
