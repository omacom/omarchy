#!/bin/bash

# omarchy:summary=Restore saved Thunderbolt firmware boot permissions

set -euo pipefail

gum confirm "Restore the previous Thunderbolt firmware boot allowlists? Device approval will remain enabled." || exit 0
sudo /usr/bin/omarchy-thunderbolt-authorization-admin boot-disable
echo "Previous firmware boot permissions restored and verified."
