#!/bin/bash

# omarchy:summary=Retry T1Bridge machine-data import once
# omarchy:hidden=true

set -uo pipefail

pkexec /usr/bin/omarchy-t1bridge-import-attempt >/dev/null 2>&1 || true
omarchy-t1bridge-import-notify --force
