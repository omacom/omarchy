#!/bin/bash

# omarchy:summary=Detect Acer Aspire Go 15 series laptops.

dmi_vendor="${OMARCHY_DMI_SYS_VENDOR:-/sys/class/dmi/id/sys_vendor}"

grep -qi "Acer" "$dmi_vendor" 2>/dev/null && {
  omarchy-hw-match "Aspire AG15-42P" || omarchy-hw-match "Aspire Go 15"
}
