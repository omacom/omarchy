#!/bin/bash

# omarchy:summary=Remove 1Password and its Brave Origin and Chromium extensions.
# omarchy:requires-sudo=true

EXTENSION_ID="aeblfdkhhhdcdjpifhhbdiojplfjncoa"
EXTENSION_FILE="/usr/share/chromium/extensions/$EXTENSION_ID.json"

sudo rm -f "$EXTENSION_FILE"
rm -f "$HOME/.config/BraveSoftware/Brave-Origin/External Extensions/$EXTENSION_ID.json"
omarchy-pkg-drop 1password 1password-cli

echo ""
echo "1Password has been removed."
