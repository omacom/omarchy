# Candidate server package set; ISO integration is a separate phase.
# Subtraction from omarchy-base.packages, audited by server-edition-test.sh.
# Every base package must be retained below or recorded as dropped here.
# Server-only addition: openssh (SSH access).
# Dropped base packages (GUI, desktop services, media, and extra toolchains):
# dropped: aether
# dropped: alsa-utils
# dropped: asdcontrol
# dropped: avahi
# dropped: bluez
# dropped: bluez-tools
# dropped: bluez-utils
# dropped: bolt
# dropped: brightnessctl
# dropped: chromium
# dropped: clang
# dropped: cliamp
# dropped: cups
# dropped: cups-filters
# dropped: cups-pk-helper
# dropped: ddcutil
# dropped: dotnet-runtime
# dropped: evince
# dropped: exfatprogs
# dropped: expac
# dropped: fcitx5
# dropped: fcitx5-gtk
# dropped: fcitx5-qt
# dropped: ffmpegthumbnailer
# dropped: fontconfig
# dropped: foot
# dropped: gnome-disk-utility
# dropped: gnome-keyring
# dropped: gnome-themes-extra
# dropped: gpu-screen-recorder
# dropped: grim
# dropped: gvfs-mtp
# dropped: gvfs-nfs
# dropped: gvfs-smb
# dropped: herdr
# dropped: hyprland
# dropped: hyprland-guiutils
# dropped: hyprland-preview-share-picker
# dropped: hyprpicker
# dropped: hyprsunset
# dropped: imagemagick
# dropped: imv
# dropped: kdenlive
# dropped: kernel-modules-hook
# dropped: libreoffice-fresh
# dropped: libsecret
# dropped: libvips
# dropped: libyaml
# dropped: llvm
# dropped: localsend
# dropped: lua51
# dropped: luarocks
# dropped: mariadb-libs
# dropped: moonlight-qt
# dropped: mpv
# dropped: mpv-mpris
# dropped: nautilus
# dropped: nautilus-python
# dropped: noto-fonts
# dropped: noto-fonts-cjk
# dropped: noto-fonts-emoji
# dropped: nss-mdns
# dropped: obs-studio
# dropped: obsidian
# dropped: omacalc
# dropped: omacut
# dropped: omawrite
# dropped: pamixer
# dropped: pinta
# dropped: plymouth
# dropped: postgresql-libs
# dropped: power-profiles-daemon
# dropped: python-gobject
# dropped: python-poetry-core
# dropped: qemu-user-static-binfmt
# dropped: qrencode
# dropped: qt6-imageformats
# dropped: qt6-multimedia
# dropped: qt6-multimedia-ffmpeg
# dropped: quickshell
# dropped: ruby
# dropped: sddm
# dropped: slurp
# dropped: socat
# dropped: sushi
# dropped: system-config-printer
# dropped: tensaku
# dropped: tesseract
# dropped: tesseract-data-eng
# dropped: tobi-try
# dropped: tree-sitter-cli
# dropped: ttf-ia-writer
# dropped: ttf-jetbrains-mono-nerd-basic
# dropped: ttfx
# dropped: tzupdate
# dropped: udiskie
# dropped: usage
# dropped: uwsm
# dropped: wireless-regdb
# dropped: wireplumber
# dropped: wl-clipboard
# dropped: woff2-font-awesome
# dropped: wtype
# dropped: xdg-desktop-portal-gtk
# dropped: xdg-desktop-portal-hyprland
# dropped: xdg-terminal-exec
# dropped: xournalpp
# dropped: yaru-icon-theme
# dropped: yt-dlp
# dropped: zbar
# lazyjournal remains opt-in until package availability is established.
bash-completion
bat
btop
docker
docker-buildx
docker-compose
dosfstools
dua-cli
eza
fakeroot
fastfetch
fd
fzf
git
gum
inetutils
inotify-tools
inxi
networkmanager
jq
lazydocker
lazygit
less
man-db
mise-bin
nvim
omarchy-nvim
openssh
pacman-contrib
plocate
ripgrep
starship
tldr
tmux
ufw
ufw-docker
unzip
vi
whois
yay
zoxide
